using System;

namespace CvAut
{
    /// <summary>
    /// Minimal entry point after UI removal.
    /// </summary>
    class Program
    {
        static void Main(string[] args)
        {
            // No UI. Application can be started via other entry points if needed.
        }
    }
}
