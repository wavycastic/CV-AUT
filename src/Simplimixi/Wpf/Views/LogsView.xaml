<UserControl x:Class="CvAut.WpfApp.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:services="clr-namespace:CvAut.WpfApp.Services"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="760">
    <Grid Style="{StaticResource PageRevealStyle}" Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:TextBlock Grid.Column="0"
                          VerticalAlignment="Center"
                          FontTypography="Title"
                          Text="Logs" />

            <ui:Button Grid.Column="1"
                       Height="32"
                       MinWidth="72"
                       Padding="12,0"
                       VerticalAlignment="Center"
                       Appearance="Secondary"
                       Content="Clear"
                       Click="OnClearClick" />
        </Grid>

        <Border Grid.Row="1"
                Padding="12"
                Background="#E80B111C"
                BorderBrush="{StaticResource GlassCardStrokeBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <RichTextBox x:Name="LogsRichTextBox"
                         Background="#CC050A12"
                         Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                         Style="{StaticResource TerminalRichTextBoxStyle}"
                         services:RichTextBoxHelper.LogText="{Binding LogsText, Mode=OneWay}" />
        </Border>
    </Grid>
</UserControl>
