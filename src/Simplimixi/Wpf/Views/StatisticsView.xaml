<UserControl x:Class="CvAut.WpfApp.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="520">
    <Grid Margin="12,8,12,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="16,8,16,12">
            <ui:TextBlock FontTypography="Title" Text="Statistics" />
            <ui:TextBlock Margin="0,2,0,0"
                          Appearance="Secondary"
                          FontTypography="Caption"
                          Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                          Text="Session loot, attacks and bot performance overview." />
        </StackPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16,4,16,16" MaxWidth="430" HorizontalAlignment="Stretch">
                <Border Style="{StaticResource GamingCardStyle}" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <ui:TextBlock FontTypography="Caption" Foreground="{ui:ThemeResource AccentTextFillColorPrimaryBrush}" Text="LIVE SESSION" />
                            <ui:TextBlock Margin="0,4,0,0" FontTypography="BodyStrong" Text="Performance snapshot" />
                            <ui:TextBlock Margin="0,4,0,0" FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding UptimeText, StringFormat=Uptime {0}}" />
                        </StackPanel>
                        <Border Grid.Column="1" Width="54" Height="54" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="12">
                            <ui:SymbolIcon HorizontalAlignment="Center" VerticalAlignment="Center" Symbol="Poll24" FontSize="28" Foreground="{ui:ThemeResource AccentTextFillColorPrimaryBrush}" />
                        </Border>
                    </Grid>
                </Border>

                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="8" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Padding="12" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <Image Width="24" Height="24" HorizontalAlignment="Left" Source="pack://siteoforigin:,,,/Templates/icon_gold.png" />
                            <ui:TextBlock Margin="0,8,0,0" FontTypography="Subtitle" Text="{Binding GoldGainedText}" />
                                                        <ui:TextBlock FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding AvgGoldPerHourText}" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="0" Grid.Column="2" Padding="12" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <Image Width="24" Height="24" HorizontalAlignment="Left" Source="pack://siteoforigin:,,,/Templates/icon_elixir.png" />
                            <ui:TextBlock Margin="0,8,0,0" FontTypography="Subtitle" Text="{Binding ElixirGainedText}" />
                                                        <ui:TextBlock FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding AvgElixirPerHourText}" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="2" Grid.Column="0" Padding="12" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <Image Width="24" Height="24" HorizontalAlignment="Left" Source="pack://siteoforigin:,,,/Templates/icon_de.png" />
                            <ui:TextBlock Margin="0,8,0,0" FontTypography="Subtitle" Text="{Binding DarkElixirGainedText}" />
                                                        <ui:TextBlock FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding AvgDarkElixirPerHourText}" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="2" Grid.Column="2" Padding="12" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <ui:SymbolIcon Symbol="Shield24" FontSize="24" Foreground="{StaticResource ReadyBrush}" />
                            <ui:TextBlock Margin="0,8,0,0" FontTypography="Subtitle" Text="{Binding AttacksCountText}" />
                                                        <ui:TextBlock FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Attacks" />
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Style="{StaticResource GamingCardStyle}" Padding="14">
                    <StackPanel>
                        <ui:TextBlock FontTypography="Caption" Foreground="{ui:ThemeResource AccentTextFillColorPrimaryBrush}" Text="SESSION BREAKDOWN" />
                        <Border Margin="0,12,0,0" Padding="12" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ui:TextBlock Grid.Column="0" FontTypography="BodyStrong" Text="Attack success" />
                                    <ui:TextBlock Grid.Column="1" FontTypography="BodyStrong" Foreground="{StaticResource ReadyBrush}" Text="{Binding SuccessRateText}" />
                                </Grid>
                                <ProgressBar Margin="0,10,0,0" Height="6" Value="{Binding SuccessRateValue, Mode=OneWay}" Maximum="100" />
                            </StackPanel>
                        </Border>
                        <Border Margin="0,10,0,0" Padding="12,10" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Margin="0,0,10,0" Symbol="DataHistogram24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                <ui:TextBlock FontTypography="BodyStrong" Text="{Binding StarBreakdownText}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
