using System.Windows.Controls;

namespace CvAut.WpfApp.Views
{
    /// <summary>
    /// Logic xử lý (Code-Behind) của StatisticsView.xaml.
    /// Quản lý giao diện hiển thị các chỉ số thống kê tài nguyên, tỷ lệ thắng, uptime và star breakdown.
    /// </summary>
    public partial class StatisticsView : UserControl
    {
        /// <summary>
        /// Khởi tạo một thực thể mới của StatisticsView.
        /// </summary>
        public StatisticsView()
        {
            InitializeComponent();
        }
    }
}
