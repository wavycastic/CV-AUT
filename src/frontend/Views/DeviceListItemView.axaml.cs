using Avalonia.Controls;

namespace CvAut.Views
{
    /// <summary>Compact row for one device in a list/grid (binds a <c>DeviceViewModel</c>).</summary>
    public partial class DeviceListItemView : UserControl
    {
        public DeviceListItemView()
        {
            InitializeComponent();
        }
    }
}
